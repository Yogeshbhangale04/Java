<%@ page language="java" contentType="text/html; charset=ISO-8859-1"
    pageEncoding="ISO-8859-1"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="ISO-8859-1">
<title>Insert title here</title>
</head>
<body>

<h1>Welcome to JSP</h1>
<form action="output.jsp">
    <label>Enter number</label>
    <input type="text" name="number">
    <button type="submit">Submit</button>
</form>

<%!
public int cube(int n){
	return n*n*n;
}
%>
<%
// Retrieve the input number from the request parameter "number"
String n = request.getParameter("number");

if (n != null && !n.isEmpty()){
    try {
        // Convert the input string to an integer
        int no = Integer.parseInt(n);
        // Print the input number to the HTML page
        out.println("Number Entered is : " + no);
        out.println("<br> <br>");
        // Calculate the cube of the input number and print it to the HTML page
        out.println("Cube of Number : " + cube(no));
    } catch (NumberFormatException e) {
        out.println("Invalid input! Please enter a valid number.");
    }
}
%>

</body>
</html>
